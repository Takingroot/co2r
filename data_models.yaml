#
# organization
#
name: String
logo_url: String URL
bio: String HTML
contact_methods: [{}]
  type: String (email, twitter, facebook, other)
  link: String Email || URL



#
# artifact
#
name: String
slug: String (= slugified name)
offset_since: Number
description: String HTML
thumbnail_url: URL

phrases:
  for_total_made: String HTML
  co2_per_thing_made: String HTML

images: [{}]
  urls:
    large: String URL
  caption: String HTML

organization: {organization}

reports: [{}]
  year: Number
  total_co2: Number
  offset_co2: Number
  co2_per_thing_made: Number
  co2_sources: [{}]
    label: String
    percentage: Number
  other_eco_actions: [{}]
    type: String (cms= combobox of: existing types or add new type )
    actions: [String HTML]


#
# faq
#
question: String HTML
slug: String (= slugified question)
answer: String HTML


#
# defined term
#
<term_name>String: {}
  title: String HTML
  cotnent: String HTML

#
# app
#
default_user_preferences:
  show_introduction: Bool

defaults:
  co2_per_thing_made_unit: 'kg'

calculation_variables:
  offsets_per_co2_ton: Number
  trees_per_offset: {}
    <year>: Number

co2_artifact_comparisons: [{}]
  phrase: String
  type: String
  co2_amuont_unit: String
  co2_amount: Number
