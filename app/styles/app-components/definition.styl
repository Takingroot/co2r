

.co2r-definition
  hlColor = fade-out(grayLight, 0.9)
  background-color hlColor
  box-shadow 0 0 0 0.33em hlColor
  color inherit
  // undo default link styling
  border-bottom none!important
  &:after
    // info icon
    content "\f05a"
    font-family FontAwesome
    font-size 0.75em
    margin-left 0.1em
  &:hover
    hlColorHover = fade-in(hlColor, 0.75)
    background-color hlColorHover
    box-shadow 0 0 0 0.33em hlColorHover
    color baseTextColor
