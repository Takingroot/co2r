


.sb
  font-size 0.9rem
  position absolute
  z-index 100
  width 100%
  &.is-scrollfix
    position fixed
    bottom -31px!important
    bottom 0
    .sb__inner
      background-color dark
      color textColorLight
.sb__text
  // fix vertical centering
  margin-bottom -0.1em

.sb__inner
  padding gd(0.2) gd(0.5)
  transition background-color 0.125s
  position absolute
  bottom gd(1/2) + 1px
  background-color grayMuted
  display inline-block

  /* Try to alleviate an empty-space problem:
   * 1. addthis appends an invisible element to the list
   *   making it impossible to rely on last-child no-gutter styling
   * 2. addthis twitter button has an overly-wide width
   *
   * The result is a large empty right-side space
   */
  padding-right 0

.sb__buttons
  margin-left gd(1)
  line-height 0
  .addthis_button_facebook_like iframe
    height 20px!important



// On an artifact page
// ------------------------

body.artifact
  .sb-global
    display none!important
